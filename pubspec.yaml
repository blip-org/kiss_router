name: kiss_router
description: A better navigation for your Flutter project
version: 0.0.1
homepage: https://github.com/blip-org/kiss_router
publish_to: none

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  modal_bottom_sheet:
    path: "./modal_bottom_sheet/modal_bottom_sheet"
  flutter_bloc: ^7.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
